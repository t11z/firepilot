# INVALID: firepilot.yaml with a role value not in the controlled vocabulary.
#
# Expected failure: JSON Schema enum validation (Gate 1)
# Violation: role "extranet" is not one of the eight allowed values defined
#   in ci/schemas/firepilot-config.schema.json (ADR-0008).

schema_version: 1
scm:
  default_folder: "Shared"
  default_position: "pre"
rule_defaults:
  tag: "firepilot-managed"
  log_end: true
  log_start: false
zones:
  external-partner:
    # INVALID: "extranet" is not in the controlled role vocabulary
    role: extranet
    description: "External partner network"
  trust:
    role: internal
    description: "Internal trusted corporate network"
